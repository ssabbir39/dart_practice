void main(){

  var x = 10;

  // when we need increment we use ++
  // when we need decrement we use --


  // when we use unarary oparator sign after variable like x++ then initially we see main value and then one by one increment

  print(x++);
  print(x++);
  print(x++);
  print(x++);

  // when we use unarary oparator sign before variable like ++x then we dont see initial value, automatically increment one by one

  // print(++x);
  // print(++x);
  // print(++x);
  // print(++x);


  // when we use unarary oparator sign after variable like x-- then initially we see main value and then one by one decrement

  // print(x--);
  // print(x--);
  // print(x--);
  // print(x--);

  // when we use unarary oparator sign before variable like --x then we dont see initial value, automatically decrement one by one

  // print(--x);
  // print(--x);
  // print(--x);
  // print(--x);

}